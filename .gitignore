# build dirs
/build/
/builddir/

# meson subproject checkouts (fetched by the wrap; packagefiles are tracked)
/subprojects/libreac/
/subprojects/*/
!/subprojects/packagefiles/
!/subprojects/*.wrap

# editor / scratch
*.o
*.a
*.swp

# rpm build artifact
reac-pw-*.tar.gz
