# Overnight status — 2026-06-14 (for review)

Autonomous run while you were away. Goal: update docs with the JOIN/HOLD
findings, fold the PipeWire version into reac-aes67, and produce both targets —
the **OpenWrt apk** (router) and the **Fedora rpm** (MiniPC PC). Nothing was
pushed; public repos are committed locally and wait for your sanitization gate.

## What landed

**1. Docs — the connection FSM is consolidated (lives the "write it down" rule).**
`reac-firmware-re/REAC-CONNECTION-FSM.md`: the virtual-stagebox JOIN/HOLD spec —
role + MAC/direction discipline, the wire/checksum reference, the RX+TX
control-frame table with evidence grades, the 5-state FSM, timers/budgets, the 8
ranked blocking gaps, the adversarial refusal/drop punch-list, and the single rig
capture that closes JOIN. `VIRTUAL-REAC-STAGEBOX.md` now points at it. (commit
`9a66998` in reac-firmware-re)

**2. JOIN/HOLD code — `reac_ctrl` + `reac_fsm` in reac-pw (private), tested 4/4.**
- `reac_ctrl`: the cdea/cfea checksum, a classifying parser (learns `master_mac`
  from the L2 source, not the disputed cfea payload), and the stagebox builders.
  Byte geometry **ground-truthed against the real captures** — I dissected
  `wired-reac-a-bothdirs`: master frames 1492 B broadcast, box frames 628 B
  unicast (`18 hdr + 32 descriptor 00 7a×16 + n_ch×36 audio + C2 EA`), checksum
  cross-checked to the on-wire box-heartbeat value `0x7a`.
- `reac_fsm`: the pure-function state machine. HOLD is sound (heartbeat re-arms
  the 600-frame countdown; peer-gone + MAC-change drop). JOIN reaches ESTABLISHED
  only on a real grant. (commit `b8f3882`)

**3. PC version — Fedora rpm, BUILT + verified.**
`reac-pw-0.1.0-1.fc43.x86_64.rpm` → `/usr/bin/reac-pw` + a systemd unit
(`AmbientCapabilities=CAP_NET_RAW`, RT limits) + `/etc/reac-pw/reac-pw.conf`.
Self-contained tarball (vendors libreac + the reac-aes67 core, no network). Unit
tests run in `%check`. (commit `0cafddf`)
```
sh packaging/make-tarball.sh && cp reac-pw-0.1.0.tar.gz ~/rpmbuild/SOURCES/ && rpmbuild -bb --nodeps packaging/reac-pw.spec
sudo dnf install ~/rpmbuild/RPMS/x86_64/reac-pw-0.1.0-1.fc43.x86_64.rpm   # then: edit /etc/reac-pw/reac-pw.conf, systemctl enable --now reac-pw
```

**4. OpenWrt version — apk intact (current `reac-aes67-0.3.0-r1.apk`).**
My integration was additive (a new `pipewire/` dir); the bridge `src/` and
`openwrt/` are untouched (git-confirmed). A clean containerized rebuild
(`scripts/build.sh`, podman + the cached SDK) **succeeded** — OpenWrt 25.12.4,
target mediatek/filogic — producing `reac-aes67-0.3.0-r1.apk`,
`libreac0-0.1.0-r1.apk`, `luci-app-reac-aes67-0.2.1-r1.apk` in `.build/out/`.
```
cd reac-aes67-pub && CONTAINER_ENGINE=podman sh scripts/build.sh   # apks land in .build/out/
```

**5. Integration — the PipeWire version folded into reac-aes67.**
`reac-aes67-pub/pipewire/` (= FreeREAC/reac-aes67) now carries the PipeWire-native
endpoint (RX `reac:capture` + TX `reac:playback`), sharing the decode core in
`../src`. Builds clean against system libpipewire. (commit `2ee1a78`, **not
pushed**)

## Evidence honesty — what will and won't link a real desk

- **HOLD (steady-state) is captured ground truth** — the heartbeat, the
  600-frame link-check, the frame geometry/checksum are byte-verified against
  20 000+ real frames. The FSM's HOLD half is validated offline.
- **JOIN (cold-connect) is reconstructed**, not byte-verified. The grant-burst
  `cdea 04 03` is absent from every steady-state capture (it was only ever in the
  lost `/tmp/hs1.pcap` / `/tmp/dgs.pcap`, surviving as the §13b/§13d transcription).
  So a real M-5000 linking on the first try is **not guaranteed** — the JOIN
  builders are flagged experimental.

## §4.2 decision I made (reversible — your call)

The JOIN/HOLD code (`reac_ctrl`/`reac_fsm`) is stagebox impersonation derived
from RE. I kept it **out of the public `reac-aes67-pub`** (it lives in private
reac-pw + reac-firmware-re); the public `pipewire/` has the bridge only. See
`reac-aes67-pub/pipewire/CONNECTION-FSM.md`. **Nothing public was pushed** —
review + decide whether to publish the FSM before any push.

## The one rig capture that closes JOIN

Capture at the **master side** during a **stagebox unplug+replug**, both
directions, `ether proto 0x8819`, ≥120 s spanning the replug. It yields the
grant-burst bytes, the TX-mute dwell, and the per-rate cadence — converting the
top blocking gaps from reconstructed to ground truth. **Commit it to
`reac-captures`, not `/tmp` this time.**

## Not done (next steps)

- Live `rx_loop → reac_ctrl_parse → reac_fsm_step → emit` wiring (the FSM is a
  tested pure function; hooking it to the socket is the runtime glue — needs the
  rig to validate the JOIN edge anyway).
- The upstream channel-slot map (task #61) for faithful input placement.
- Confirm the explicit-disconnect byte (left unwired — the master HB `[22]` is a
  channel#, not a keep-alive flag).

## Commits (none pushed)

- reac-firmware-re `9a66998` — connection FSM spec
- reac-pw `b8f3882` (ctrl+fsm), `0cafddf` (rpm packaging), plus earlier
  `382501f`/`1c6d028`/`60790d5` (ring trim, TX encoder, sink)
- reac-aes67-pub `2ee1a78` — pipewire fold-in (**public, hold for sanitize gate**)
