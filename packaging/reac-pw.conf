# reac-pw service configuration (Fedora MiniPC).
# REAC_IFACE: the NIC carrying the REAC 0x8819 stream (the wired link to the desk).
# REAC_RATE:  44100 | 48000 | 96000  (0 / unset = auto-detect on the live socket).
REAC_IFACE=reac0
REAC_RATE=48000
